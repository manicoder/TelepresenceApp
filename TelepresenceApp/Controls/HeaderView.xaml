<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GCoreApp.Controls.HeaderView" BackgroundColor="{StaticResource PrimaryColor}">
    <ContentView.Content>
        <Grid Padding="20,40,20,16" HorizontalOptions="FillAndExpand" 
                      VerticalOptions="FillAndExpand">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ImageButton BackgroundColor="Transparent" HorizontalOptions="Start" VerticalOptions="Center"
                         Command="{Binding GoHomePageCommand}" HeightRequest="30" WidthRequest="30"
                         Source="back" Aspect="AspectFit"/>
            <Label x:Name="title"  Grid.Column="1" Text="{Binding Title}" TextColor="White" FontSize="20" HorizontalTextAlignment="Center" VerticalTextAlignment="Center"
                   Style="{StaticResource TitleBold}" />
            <Image Grid.Column="2" BackgroundColor="Transparent" HeightRequest="30" WidthRequest="30"
                         VerticalOptions="Center" HorizontalOptions="End"
                          Source="location"
                         Aspect="AspectFit"/>

        </Grid>

    </ContentView.Content>
</ContentView>